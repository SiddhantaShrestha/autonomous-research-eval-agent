# Vercel / production: URL of the Python API (Render). No trailing slash.
# Example: https://research-eval-api.onrender.com
# Leave unset for local dev (uses Next.js /api/evaluate + local Python).
NEXT_PUBLIC_API_URL=
