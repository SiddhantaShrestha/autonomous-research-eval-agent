GROQ_API_KEY=your_groq_api_key_here

# Render (FastAPI) — comma-separated browser origins allowed to call the API (CORS).
# Example: https://your-app.vercel.app,http://localhost:3000
# If unset, the API allows any origin (fine for trying locally; set explicitly in production).
ALLOWED_ORIGINS=